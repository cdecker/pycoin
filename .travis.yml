language: python
python:
  - "2.7"

install:
  - pip install python-coveralls coverage
  - pip install -r requirements.txt

# command to run tests
script:
  - python setup.py test
  - coverage run --source=bitcoin setup.py test

after_success:
  - coveralls
